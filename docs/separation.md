# 분리 기준

기존 공개 운영본 `10d3b7d7e3ce938831cea32e850c58038c71013f`에서 새 이력으로 시작했다. 원래 저장소의 Git 이력과 `.env`, DB·백업·로그·운영 인증 자료는 가져오지 않았다. 브라우저 화면과 서버 API를 각 프로세스로 실행하며 서비스/관리자 세션을 서로 재사용하지 않는다.

운영 이전은 별도 단계다. 현재 도메인과 기존 배포를 보존한 상태에서 새 저장소들의 빌드·API·브라우저 호환성을 검사한다.
