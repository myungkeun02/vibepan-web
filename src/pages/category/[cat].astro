---
import type { PageData } from '../../contracts/category/[cat]';
import Base from '../../layouts/Base.astro';
import Directory from '../../components/Directory.astro';
import Mascot from '../../components/Mascot.astro';
const { cat, params, list, jsonld } = Astro.locals.presentation.data as PageData;
---

<Base
  title={cat ? cat.name + ' 도구, 직접 만들 수 있을까?' : '카테고리를 찾을 수 없어요'}
  {jsonld}
  noindex={!cat}
  ><div class:list={['page-head', { empty: !cat }]}>
    {!cat && <Mascot size="large" />}
    <div class="eyebrow">분야별 도구 찾기</div><h1>
      {cat ? cat.emoji + ' ' + cat.name : '카테고리를 찾을 수 없어요'}
    </h1><p>
      {
        cat
          ? '직접 만들 수 있는 기능과 만들기 어려운 기능을 비교해 보세요.'
          : '주소를 확인하거나 전체 도구에서 다시 찾아보세요.'
      }
    </p>
    {
      !cat && (
        <a href="/" class="button">
          도구 탐색으로 돌아가기
        </a>
      )
    }
  </div>{cat && <Directory {params} />}</Base
>
